aufgabe:
- modularisieren
 x Routes in extra files
 x mit router einbinden in app.js
 x validierung in extra file also alle json schemen

- alles in einem docker container
 x rest service und db
 x docker-compose file

- komplexe db
 x ca 15 tabellen
 x Bilder speichern

plus doku

was fehlt:
bilder speichern
swagger doku
vllt beispiel postman abfragen
json schema für alle tabellen